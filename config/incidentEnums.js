/**
 * ? Allowed values for an incident's severity and status, used by both
 * ? the incident schema and the import transform files
 */

export const severityValues = ['high', 'medium', 'low', 'none'];
export const statusValues = ['pending', 'ongoing', 'resolved'];
